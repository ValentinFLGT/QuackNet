{% extends 'base.html.twig' %}

{% block title %}Edit Quack{% endblock %}

{% block body %}

    <h1>Edit Quack</h1>

    {{ include('quack/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row">

        <a href="{{ path('quack_show', {'id': quack.id }) }}" class="col-2">
            <button class="btn btn-outline-warning">Back to Quack</button>
        </a>

        {{ include('quack/_delete_form.html.twig') }}

    </div>

{% endblock %}
